server:
  port: 9001

spring:
  application:
    name: gradu-qa

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gra_qa?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: "000000"

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
